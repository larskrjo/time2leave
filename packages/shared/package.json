{
  "name": "@time2leave/shared",
  "version": "0.0.0",
  "private": true,
  "description": "Pure-TS code shared between the web SPA and the Expo mobile app: types, API client factory, time + slot helpers.",
  "type": "module",
  "main": "./src/index.ts",
  "types": "./src/index.ts",
  "exports": {
    ".": "./src/index.ts"
  },
  "scripts": {
    "typecheck": "tsc --noEmit",
    "test": "vitest run"
  },
  "devDependencies": {
    "typescript": "^5.9.2",
    "vitest": "^3.2.4"
  }
}
